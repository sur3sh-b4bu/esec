# AGENT SECURITY & SAFETY RULES

> [!CAUTION]
> **STRICT SECURITY RULE FOR ALL AGENTS (Claude, Antigravity, and all AI tools):**
> - **NEVER** read, view, edit, modify, create, delete, or interact with any `.env` files (including `.env`, `.env.local`, `.env.development`, `.env.production`, etc.).
> - **NEVER** read, view, edit, modify, create, delete, or interact with any `environment` directories or files (including `src/environments/`, `environment.ts`, `environment.prod.ts`, `environment.development.ts`, etc.).
> - This restriction applies strictly to all AI agents across all steps and tasks.

